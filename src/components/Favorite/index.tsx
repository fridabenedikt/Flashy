/* 
import { doc, updateDoc, arrayUnion } from 'firebase/firestore';
import { db } from "~/src/api/firebase-config";  */

const Favorite = () => {
  /* const addStringToArray = async (collectionName: string, docId: string, arrayFieldName: any, stringValue: unknown) => {
    const docRef = doc(db, collectionName, docId);

    try {
      await updateDoc(docRef, {
        [arrayFieldName]: arrayUnion(stringValue)
      });
      console.log('String added to array successfully');
    } catch (error) {
      console.error('Error adding string to array:', error);
    }
  }; */
};

export default Favorite;
